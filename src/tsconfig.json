{
  "compilerOptions": {
    "module": "es6",
    "inlineSourceMap": true,
    "outDir": "../site",
    "sourceRoot": ".", // Needed since we copy the TS files over to the site directory.
    "rootDir": "."
  },
  "files": ["index.ts"]
}
